# EVA ICS MCP service — deployed automatically by fendtastic
# See: https://info.bma.ai/en/actual/eva4/svc/eva-mcp.html#setup
command: svc/eva-mcp
workers: 1
bus:
  path: var/bus.ipc
config:
  # Bind to 0.0.0.0 so it is reachable from outside the container
  listen: 0.0.0.0:8765
  # Allow all EAPI methods (restrict in production)
  allow: "*"
  #allow:
  #  eva.core:
  #    - test
  #    - item.list
  #    - item.state
  #  eva.repl.default:
  #    - test
  #    - node.list
  #  eva.svc.test: "*"
user: nobody
